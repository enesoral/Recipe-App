spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_prod
    username: prod_user
    password: *****
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLInnoDBDialect
    database: mysql
    show-sql: false